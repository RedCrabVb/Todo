import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        padding: 30,
        maxWidth: 500,
        minWidth: 300
    },
    textBig: {
        fontSize: 26,
        fontWeight: 'bold',
        textAlign: 'center'
    }
});
